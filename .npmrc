# Keep dependency resolution strict so packages must declare what they import.
# This is what protects the one-way dependency rule between web / api / domain.
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
