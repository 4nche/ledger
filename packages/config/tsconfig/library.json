{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Library (pure TypeScript, no runtime assumptions)",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "outDir": "dist",
    "rootDir": "src"
  }
}
